# セキュリティ監査レポート

- 監査日: 2026-08-07
- 対象: 駐車券記録アプリ（PHP 8.3 + PDO SQLite + Bootstrap 5.3.3 CDN、Docker デプロイ）
- 方法: 静的レビュー（全ソース）+ 稼働中コンテナへの攻撃的アクセステスト（許可済み侵入テスト）
- 詳細版: [docs/compose/reports/security-audit-2026-08-07.md](docs/compose/reports/security-audit-2026-08-07.md)

---

## 総評

**クリティカル・高危険度の脆弱性は検出されませんでした。** SQL インジェクション・XSS・CSRF・セッションフィクセーション・DB ファイルの直接アクセスはすべて防御が機能していることを実測で確認しました。検出されたのは「運用上の注意」レベルの 4 件（F1〜F4）で、**すべて修正済み**です（2026-08-07・修正後の再検証も全スイート PASS）。

## 監査方法

| 項目 | 内容 |
|---|---|
| 静的レビュー | 全ソース読解（api.php / lib/* / index.php / Dockerfile / entrypoint.sh）。OWASP Top 10 ベースで SQLi・XSS・認証・セッション・CSRF・情報開示・ファイルアクセスを確認 |
| 動的テスト | 稼働中コンテナ（park-app @ 127.0.0.1:4500）へ curl で攻撃プローブ。SQLi / パラメータ注入 / 機密ファイルアクセス / パストラバーサル / メソッド乱用 / ブルートフォース / セッションフィクセーション |
| ランタイム検査 | `docker exec` で PHP ini・Apache 設定を実測。data/.htaccess の実効性は「退避→即復元」テストで決定的に実証 |

## テスト結果サマリ（実測）

| # | テスト | 結果 |
|---|---|---|
| 1 | SQLi: `action=today'`・`action=drop`・`monthly year=2026' OR '1'='1` 等 | 全て 400/404・DB 無傷 |
| 2 | パラメータ注入: count=1000/-5/3.5/文字列/null・year=1999・month=13 | 全て 400（型・範囲検証が実効） |
| 3 | 機密ファイル: /.git/config・/Dockerfile・/lib/*.php 直GET | 404 または空ボディ（ソース漏洩なし） |
| 4 | DB 直アクセス: /data/parking.db・/data/.htaccess | 403（.htaccess の実効を退避テストで確認） |
| 5 | パストラバーサル: `/%2e%2e/etc/passwd` | 400 |
| 6 | メソッド乱用: TRACE / PUT / GET-add | 405 / 200（副作用なし）/ 400 |
| 7 | ブルートフォース: 誤PW 5回 → 正解 | 全て 401 → 200（当時はレート制限なし → **F1**） |
| 8 | セッションフィクセーション: ログイン前後でセッションID | 再生成を確認（防御OK） |
| 9 | display_errors / expose_php / ServerTokens | 開示あり（**F2/F3**） |
| 10 | セキュリティヘッダ（HTML） | なし（**F4**） |

## 発見と対応（F1〜F4・すべて修正済み）

| 指摘 | 内容 | 影響 | 対応 | 検証（実測） |
|---|---|---|---|---|
| **F1（中）** | ログインにレート制限・ロックアウトがない | 4桁PW（10,000通り）を LAN 内から総当たり可能 | api.php のログイン失敗時に `sleep(1)` を追加（ステートレスの最小実装） | スモーク テスト13: 誤PW応答 **1019ms**（全通り試行に最低約2.8時間） |
| **F2（低〜中）** | コンテナ PHP が display_errors=1 | 障害時にスタックトレース・絶対パスが HTTP 応答へ漏れる可能性 | Dockerfile で `display_errors = Off`・`log_errors = On`・`expose_php = Off`（エラーは docker logs へ継続） | docker_check テスト9: `docker exec` で Off を確認 |
| **F3（低）** | バージョン開示（Apache/PHP/404 ページ署名） | 既知CVE を狙う攻撃者の情報収集を助ける | Dockerfile で `ServerTokens Prod`・`ServerSignature Off` + `a2enconf`（expose_php=Off は F2 で同時解消） | docker_check テスト8/11: `Server: Apache`・X-Powered-By なし・404 署名なし |
| **F4（低）** | HTML ページにセキュリティヘッダなし | MIME スニッフィング・クリックジャッキングの残余リスク | index.php に `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY` / `Referrer-Policy: no-referrer` を追加（共有レンタルサーバーでも効く PHP 実装。CSP はインラインJS + CDN のため不採用） | スモーク テスト14 + docker_check テスト10: 3ヘッダとも検出 |

## 検証済みの防御（監査で機能を確認した実装）

- **SQL インジェクション**: 全クエリが PDO 準備文 + バインド。動的 SQL はスキーマ定数のみ。入力は型・範囲を厳格検証（is_int / ctype_digit / 1〜999 / 2000-2100 / 1-12）
- **XSS**: 描画はすべて `textContent`（innerHTML 不使用）→ ユーザー入力が HTML として解釈される経路なし
- **認証**: `hash_equals`（定数時間比較）+ ログイン時に `session_regenerate_id(true)`（セッション固定対策）+ Cookie は HttpOnly / SameSite=Lax
- **CSRF**: CORS ヘッダなし + 削除は DELETE メソッドのみ + 記録は JSON ボディ必須 → クロスサイトフォームでは再現不能
- **DB ファイル保護**: data/.htaccess（deny-all）が実効（退避テストで 200 に変化することを実証）。entrypoint が起動時に .htaccess 不在を再作成

## 修正後の再検証（2026-08-07・fresh 実行・全 PASS）

| スイート | 結果 | 証跡 |
|---|---|---|
| 単体テスト | 12/12 PASS | reports/2026-08-07-unit-test-results.txt |
| HTTPスモーク | 14/14 PASS | reports/2026-08-07-smoke-test-results.txt |
| UI E2E（ヘッドレスchrome） | 17/17 PASS（pageErrors none） | reports/2026-08-07-e2e-ui-results.txt |
| Dockerデプロイ検証 | 17/17 PASS | reports/2026-08-07-docker-verification.txt |

## 監査時の注意事項（設計上許容・変更不要）

- **PW は平文でソースに固定**（ADMIN_PW=1234、公開リポジトリに掲載）— デモ用PW としてユーザー承諾済み。実運用で露出範囲を広げる場合は必ず変更
- **記録（add）は無認証** — 仕様どおり「記録は自由」。LAN 内の誰でも枚数を記録できる（今日一覧から削除可能）
- **セッションクッキーに Secure フラグなし** — LAN 内 HTTP が前提のため正常。HTTPS 化する場合は付与

## 結論

LAN 内運用 + デモ用PW という想定の脅威モデルに対して、アプリの防御は堅牢で、監査で指摘した 4 件はすべて軽微な運用改善として修正を完了しました。修正後も全機能の回帰テスト（4 スイート・計 60 チェック）が PASS しており、実運用に支障はありません。
